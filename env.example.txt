# ArcticRoute runtime environment (env.example.txt)
# 复制为项目根的 .env 并填写。不要提交真实密钥。

# General
ARCTICROUTE_ROOT=/app
PYTHONPATH=/app
ARCTICROUTE_DATA=/data

# STAC / CDSE access (choose token OR username/password)
CDSE_TOKEN=
CDSE_USERNAME=
CDSE_PASSWORD=
# Override STAC URL if needed
MPC_STAC_URL=https://planetarycomputer.microsoft.com/api/stac/v1

# Ingest behavior
# Force HTTP mosaic to avoid s3:// in some environments
FORCE_HTTP_MOSAIC=false

# Cache options
# Extra cache dirs separated by path separator
ARCTICROUTE_EXTRA_CACHE_DIRS=

# Logging
ARCTICROUTE_LOG_FILE=outputs/pipeline_runs.log

